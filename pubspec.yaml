name: flutter_application_1
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_native_splash: ^2.4.1


flutter:
  uses-material-design: true

  assets:
    - assets/splash.png

flutter_native_splash:
  color: "#FFF5F7"          # mesma cor de fundo do app
  image: assets/splash.png  # sua lotus
  android: true
  ios: true
  web: false
  android_12:
    color: "#FFF5F7"
    image: assets/splash.png